// Re-export from the canonical sounds module so both import paths work.
export {
  unlockAudio,
  playAppOpen,
  playOrderFilled,
  playHighImpactAlert,
  playSignalArmed,
  playChatPing,
  playNotificationPing,
  preloadSounds,
} from "./sounds";
